app = "game"
# 换成你喜欢的名字，之后访问地址会是 你的应用名.fly.dev

kill_signal = "SIGINT"
kill_timeout = 5

[[mounts]]
  source = "sqlite_data"
  destination = "/data"

[http_service]
  internal_port = 3000
  force_https = true
  auto_stop_machines = true
  auto_start_machines = true
  min_machines_running = 1
